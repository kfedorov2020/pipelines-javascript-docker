module.exports = {
    testMatch: [
        '**/?(*.)+(spec|test).json'    // Include JSON test files
      ],
    reporters: [
        'default',
        ['jest-junit', { outputDirectory: './junit-results', outputName: 'test-results.xml' }]
      ]
  };
  
